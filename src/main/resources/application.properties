spring.application.name=math-sequences-api
spring.main.allow-bean-definition-overriding=true
spring.main.allow-circular-references=true

# DB Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/math_sequences
spring.datasource.username=заполнить
spring.datasource.password=заполнить
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# Liquibase
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/changelog/changelog-master.xml
spring.liquibase.default-schema=public
spring.liquibase.liquibase-schema=public

# JWT
jwt.secret=984hg493gh0439rthr0429uruj2309yh937gc763fe87t3f89723gf
jwt.lifetime=10m

# Encryptor
card.encryption.key=my-32-character-super-secret-key-1234
card.encryption.algorithm=AES

# Server
server.port=8080